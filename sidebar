<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Professional Sidebar | Elevo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Poppins", sans-serif;
    }

    body {
      display: flex;
      min-height: 100vh;
      background: #f5f7fb;
      color: #333;
      overflow-x: hidden;
    }

    /* ===== Sidebar ===== */
    .sidebar {
      width: 260px;
      background: linear-gradient(160deg, #001f3f, #005f73);
      color: #fff;
      position: fixed;
      left: -260px;
      top: 0;
      height: 100%;
      transition: all 0.4s ease;
      z-index: 1000;
      padding-top: 2rem;
      border-right: 1px solid rgba(255, 255, 255, 0.1);
      box-shadow: 0 0 25px rgba(0, 0, 0, 0.2);
      backdrop-filter: blur(10px);
    }

    .sidebar.active {
      left: 0;
    }

    .logo {
      text-align: center;
      font-size: 1.8rem;
      font-weight: 700;
      letter-spacing: 1px;
      margin-bottom: 2rem;
    }

    .logo span {
      color: #00b4d8;
    }

    .sidebar ul {
      list-style: none;
      padding: 0;
    }

    .sidebar ul li {
      padding: 14px 25px;
      margin: 8px 15px;
      border-radius: 10px;
      transition: 0.3s;
      display: flex;
      align-items: center;
      gap: 12px;
      cursor: pointer;
      font-size: 1rem;
      color: #dce3f0;
    }

    .sidebar ul li i {
      font-size: 1.3rem;
      color: #00b4d8;
      transition: 0.3s;
    }

    .sidebar ul li:hover {
      background: rgba(255, 255, 255, 0.15);
      transform: translateX(5px);
    }

    .sidebar ul li:hover i {
      color: #fff;
    }

    .sidebar ul li.active {
      background: rgba(255, 255, 255, 0.25);
      color: #fff;
    }

    .sidebar ul li.active i {
      color: #fff;
    }

    /* ===== Main Content ===== */
    .content {
      flex: 1;
      margin-left: 0;
      transition: margin-left 0.4s ease;
      padding: 3rem;
      width: 100%;
    }

    .sidebar.active ~ .content {
      margin-left: 260px;
    }

    .content h1 {
      font-weight: 700;
      color: #001f3f;
      font-size: 2.2rem;
      margin-bottom: 1rem;
    }

    .content p {
      font-size: 1.1rem;
      color: #444;
      max-width: 700px;
    }

    /* ===== Toggle Button ===== */
    .toggle-btn {
      position: fixed;
      top: 15px;
      left: 15px;
      font-size: 1.8rem;
      background: #ffffff;
      border: none;
      border-radius: 12px;
      padding: 8px 12px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      cursor: pointer;
      transition: 0.3s;
      z-index: 1100;
      color: #005f73;
    }

    .toggle-btn:hover {
      background: #005f73;
      color: #fff;
      transform: scale(1.08);
    }

    /* ===== Responsive ===== */
    @media (max-width: 768px) {
      .sidebar {
        width: 220px;
      }

      .sidebar ul li {
        font-size: 0.9rem;
      }

      .sidebar.active ~ .content {
        margin-left: 0;
      }

      .content {
        padding: 2rem;
      }
    }
  </style>
</head>
<body>
  <button class="toggle-btn" id="toggle-btn"><i class="bi bi-list"></i></button>

  <div class="sidebar" id="sidebar">
    <div class="logo">Elevo<span>Pro</span></div>
    <ul>
      <li class="active"><i class="bi bi-house-door"></i> Dashboard</li>
      <li><i class="bi bi-person-circle"></i> Profile</li>
      <li><i class="bi bi-bar-chart"></i> Analytics</li>
      <li><i class="bi bi-envelope"></i> Messages</li>
      <li><i class="bi bi-gear"></i> Settings</li>
      <li><i class="bi bi-box-arrow-right"></i> Logout</li>
    </ul>
  </div>

  <style>
  body, html {
    height: 100%;
    margin: 0;
    font-family: Arial, sans-serif;
  }

  .content {
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center; /* vertical center */
    align-items: center;     /* horizontal center */
    text-align: center;      /* center text */
    padding: 20px;
    box-sizing: border-box;
  }

  .content h1 {
    margin-bottom: 20px;
    font-size: 2.5rem;
  }

  .content p {
    max-width: 600px;
    line-height: 1.6;
    font-size: 1.1rem;
    color: #555;
  }
</style>

<div class="content">
  <h1>Welcome to ElevoPro Dashboard</h1>
  <p>Manage your analytics, profile, and settings easily using the sidebar. Resize your window to experience a fully responsive interface with smooth transitions.</p>
</div>


 <script>
  const sidebar = document.getElementById('sidebar');
  const toggleBtn = document.getElementById('toggle-btn');

  toggleBtn.addEventListener('click', () => {
    sidebar.classList.toggle('active');

    // Hide the toggle button when sidebar is active
    if (sidebar.classList.contains('active')) {
      toggleBtn.style.display = 'none';
    } else {
      toggleBtn.style.display = 'block';
    }
  });

  const links = document.querySelectorAll('.sidebar ul li');
  links.forEach(link => {
    link.addEventListener('click', () => {
      links.forEach(l => l.classList.remove('active'));
      link.classList.add('active');
    });
  });
</script>


</body>
</html>
